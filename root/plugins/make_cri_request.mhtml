<%args>
$object
</%args>

% if ($c->user_exists() # && $object->sequencing_runs()->count() == 0
%      ) {
<div>
  <a href="<% $c->uri_for('/plugin/make_cri_request', { 
           'sequencing_sample.id' => $object->sequencing_sample_id() }) %>">
    Book a sequencing run for this sequencing sample
  </a>
</div>
% }
